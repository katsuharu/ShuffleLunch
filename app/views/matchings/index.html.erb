<% provide(:title, "マッチングエントリー") %>
<% provide(:page_title, "マッチングエントリー") %>

<div class="col-lg-12">
  <div class="col-lg-8">
    <div class="bs-component">
      <%= form_tag("/waiting", method: "post", id: "matching") do %>
        <div class="form-group">
            <%= submit_tag "シャッフルランチにエントリーする", class: "btn btn-primary" %>
            
        </div>
      <% end %>
    </div>
  </div>
</div>